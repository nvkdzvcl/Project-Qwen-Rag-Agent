# Frontend
streamlit==1.41.1

# LLM & RAG pipeline
langchain==0.3.16
langchain-community==0.3.16
ollama==0.1.0  # Phiên bản tùy theo cài đặt hiện tại

# Vector search & Embedding
faiss-cpu==1.9.0
sentence-transformers==2.2.2
transformers==4.34.0  # Hỗ trợ model embedding

# PDF / DOC processing
pdfplumber==0.10.0
PyPDF2==3.0.1
python-docx==0.8.12  # Nếu muốn mở rộng hỗ trợ DOCX

# Scientific computing
numpy==1.26.0
pandas==2.1.0
torch==2.1.0

# Logging & utils
tqdm==4.65.0  # Progress bars